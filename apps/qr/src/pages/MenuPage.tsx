import React from 'react';
export default function MenuPage() {
  return (
    <div className="min-h-screen" style={{ backgroundColor: 'var(--cd-bg)' }}>
      <p className="text-center pt-24 text-sm" style={{ color: 'var(--cd-muted)' }}>
        Menu — coming in next spec
      </p>
    </div>
  );
}
